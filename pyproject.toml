[project]
name = "email-triage-openenv"
version = "0.1.0"
description = "OpenEnv email triage environment"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0.0",
    "fastapi",
    "uvicorn",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server:app"